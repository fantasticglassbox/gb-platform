import 'package:flutter/material.dart';

final appTheme = ThemeData(
    primaryColor: const Color(0xff0A77FF),
    colorScheme: ColorScheme.fromSeed(seedColor: const Color(0xff0A77FF)),
    useMaterial3: true);
